import { style } from '@vanilla-extract/css'

export const styles = {
  avatar: style({
    paddingTop: '2px',
    marginTop: '-5px',
    cursor: 'pointer',
  }),

  dropdownMenu: style({
    width: '180px',
  }),
}
