import { style } from '@vanilla-extract/css'

export const styles = {
  logo: style({
    fontSize: '1rem',
    height: '50px',
    lineHeight: '50px',
    textAlign: 'center',
    cursor: 'pointer',
  }),
}
