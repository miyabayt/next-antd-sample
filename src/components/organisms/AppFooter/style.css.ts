import { style } from '@vanilla-extract/css'

export const styles = {
  footer: style({
    height: '50px',
    lineHeight: '50px',
    padding: '0 16px',
    margin: 0,
    backgroundColor: '#fff',
  }),
}
